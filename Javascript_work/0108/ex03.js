const aa = {
  a: "변수10",
  b: "변수20",
  c: "변수30",
};

const { a, b, c } = aa;

console.log(`a=${a}`);
console.log(`b=${b}`);
console.log(`c=${c}`);

// 문자열로 만드는 함수 JSON.stringfy();
//문자열을 다시 객체로 만드는 함수 JSON.parse();
